#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    if (scanf("%d", &n) != 1) {
        return 1;
    }
    
    int sum = 0;
    
    while (n > 0) {
        sum += n % 10;  
        n = n / 10;     
    }
    
    printf("%d\n", sum);
    
    return 0;
}
